# 2026-09-16

## 修复上架驳回：description 含 "Obsidian"（v0.18.2）

**问题**：community.obsidian.md 提交报 `Plugin description must not include the word "Obsidian".`（`manifest.json:6`）。审核原文：*"The word 'Obsidian' in the description is redundant. It is implied by the context of the plugin directory."*

**改动**
- `manifest.json`：description `…directly in Obsidian.` → `…directly in your vault.`（136 字符，句号结尾）；`version` → `0.18.2`。
- `package.json` / `versions.json`：同步 0.18.2（versions.json 追加 `"0.18.2": "1.4.0"`）。
- `.github/scripts/check-version.mjs`：新增 description 四条**断言（error 级）**——含 `obsidian`、以 `This plugin…` 开头、>250 字符、结尾不是 `.?!` 或 `)`；同时给 name 补 `Obsi` 开头 / `dian` 结尾两条 warning。另打印 description 原文与字符数，本地方便肉眼复核。
- `README.md`：发布示例 tag 0.18.1 → 0.18.2。
- `tsc -noEmit -skipLibCheck` + `esbuild --production` 均通过（main.js 实际不含版本号，此改动不影响产物）。

**验证**
- `node .github/scripts/check-version.mjs` → exit 0，0 提示。
- 故意回归（`.workbuddy/tmp/regress-check.js`，把 4 种文案分别喂给 checker）：原始被驳回文案 / `This plugin…` 开头 / 结尾无标点 → 全部 exit 1 且命中红线；合法文案 → exit 0。**新断言确实会拦，不是假绿。**

**结论 / 未做**
- 上架侧还需要：HEAD 上的 manifest 更新 + 递增版本的 GitHub Release 0.18.2（官方：*"修改后发布新的 GitHub Release（递增版本号）即可，无需重新走提交流程"*，也可在目录页直接编辑 description 后点 Publish）——本轮已按前者做完，见下节。
- `package.json#description` 里的 "Obsidian" **故意保留**：那是 NPM 包描述，不在审核口径内，且与 `package.json#name` 保留 `obsidian-` 前缀的既有决策一致。
- 顺手把这条红线补进 skill `obsidian-plugin-dev`（SKILL.md 检查清单 + 踩坑速查表、`references/obsidian-official-docs.md` §4 description 规则、`release-checklist.md`、`release-workflow.md` 的 check-version 模板）——原来官方快照那条写的是「专有名词大小写正确（`Obsidian`、…）」，与实际审核口径**相互矛盾**，已改正。

## 提交 + 发版（用户指令后补记）

- commit `2a8ff6c`「fix: description 去掉 "Obsidian" 字样，修复上架驳回 (v0.18.2)」，8 files / +155 −68（含上一轮遗留未提交的 `2026-09-14.md` 追加段）。
- 附注 tag `0.18.2`（与 0.18.0 / 0.18.1 保持同类型；`git tag -a`）。**打 tag 前先跑 `check-version.mjs --tag 0.18.2`**，CI 里也有这一步守门。
- 推 `main` + tag 成功，`release.yml` 自动建 **Release 0.18.2**（`draft:false`）：
  `main.js` 1060382 / `manifest.json` **315**（0.18.1 是 309，+6 = “in Obsidian.”→“in your vault.” 的 +2 与 “Compatible with **the** draw.io format” 的 +4，符合预期）/ `styles.css` 60667。
  `release.yml` 是把 checkout 到的仓库内 `manifest.json` **原样**当资产上传（`gh release create … manifest.json`），所以资产内容 = 仓库里那份。
- **仓库已改名**：`hellokunzai/obsidian-drawio-editor` → `hellokunzai/obsidian-drawio-studio`（push 时报 "This repository moved"）。本地 `origin` 已 `set-url` 到新地址；上架表单里的 repo 字段若还是旧名需同步改。
- 剩下属上架侧、未做：去 community.obsidian.md 重新校验并 Publish。

## 清掉 `obsidianmd/no-static-styles-assignment` 全部 34 处

**触发**：用户贴来审核侧 eslint 报错，34 处 `element.style.X = "字面量"`，分布在 6 个文件（DrawPanel 3 / DrawioView 8 / FloatingPanel 3 / FormatPanel 10 / PageBar 7 / ViewMenu 3）。

**改法（按性质分两类）**
- **静态状态 → 复用已有 `.drawio-hidden` 工具类**（`display: none !important`）：customSizeRow、ctxMenuEl、toolwin、pagebar dropEl / menuEl、viewmenu。⚠️ `.drawio-pagebar-drop` 样式表里原本写着 `display: none`，必须先**删掉那行**再换类，否则摘掉工具类它仍然是隐藏的。类切换一律用原生 `classList`（见下）。
- **JS 算出来的值 → CSS 自定义属性**，新建 `src/cssVars.ts`（`setCssVars` / `clearCssVars`，内部 `style.setProperty` / `removeProperty`）：
  - 页面视图宽高 `--drawio-page-w/h`（`.drawio-page-block` 里 `width/height: var(...)`；退出时摘变量 → 回落到 `.drawio-graph-container` 的 `100%`，所以 4 条内联写法全没了）
  - 网格 `--drawio-grid-image/size/pos`（关网格 = 摘声明，回落 `none/auto/0 0`，不再写 `= "none"` / `= ""`）
  - 页签拖拽 `--drawio-tab-dx`（消费点挂在既有的 `.is-dragging` 上，`transform: translateX(var(--drawio-tab-dx, 0px))`）
- **FormatPanel**：`colorField` 的透明 `<input type=color>` 6 条静态声明搬进新规则 `.drawio-fmt-color input`；`toggleBtn` 的 bold/italic/underline 改成 `.drawio-fmt-body .drawio-fmt-tbtn.drawio-fmt-tbtn-*`（**特异性故意与 `.active` 同为 (0,3,0) 且写在它之后**，否则选中态 `font-weight: 600` 会盖掉 bold）；`paintSwatch` 把两个分支合并成「先算变量再赋一次」。
- **`setCssProps`/`setCssStyles` 不能用**：`minAppVersion` 是 1.4.0，这两个 API 1.13 才有，且 d.ts 里没有 `@since`（`no-unsupported-api` 查不出来）。

**顺手发现（本轮刻意不动）**：`toggleBtn` 的第 4 个调用点传 `"strike"`，但原代码只处理 bold/italic/underline —— 即「S」按钮从没有字形预览样式。按「保持行为不变」原则原样保留（新类名也没给它建规则），等用户决定是否补 `.drawio-fmt-tbtn-strike`。

**验证（四段）**
1. 基线复现：改前 src 命中 34 条，与用户给的行号逐个对上。
2. 修后 src = **0**（剩 293 条全在 vendor 的 `mxClient.min.js`，审核侧不扫）。
3. **同口径**前后对比（都在 `node_modules` 就位、obsidian 类型可解析的状态）：该规则 327→293（src 34→0）、`no-unsafe-call` 368→368 **零新增**、总 error 2444→2410。⚠️ 第一版对比被污染过：改前那次跑在没有 `node_modules` 的树上，obsidian 类型解析不了 → `no-unsafe-call` 虚高 744，差点误判成「改完反而多了」。
4. `no-unsupported-api` 单独跑：**先做正向对照**（临时 `src/__api-probe.ts` 引用 `CapacitorAdapter`（@since 1.7.2）→ 命中 1 条，证明规则真激活、`node_modules/obsidian/obsidian.d.ts` 被 program 收录），再确认 6 个改动文件 + `cssVars.ts` 命中 0。探针用完即删。
5. 行为等价性：`.workbuddy/tmp/cssvar-probe.html` 直接 `<link>` 真 styles.css，**同页并列跑「旧内联写法」与「新写法」比对 computed 值** → 42/42 PASS。只复述期望值的写法会踩浏览器归一化的坑（`to bottom` 被省略、`transparent`→`rgba(0,0,0,0)`、`background-size` 按层重复），等价性断言才靠得住。
6. 按钮特异性全量扫描退出码 0；`tsc --noEmit` 通过；`esbuild --production` 出 `main.js` 1066546 字节（含全部新类名 / 变量名）。

**环境上的坑（本轮新踩）**
- 项目里**没有 `node_modules`**（`.gitignore` 里也忽略它）→ tsc / esbuild 都跑不了。做法：在托管 node 工作区装依赖，再在项目根建一个 **junction `node_modules` → `C:\Users\hellokunzai\.workbuddy\binaries\node\workspace\node_modules`**，于是 `node esbuild.config.mjs --production` 与文档里的流程完全一致。依赖版本按 `package.json` 声明对齐：`typescript@5.4.5` / `esbuild@0.20.2` / `pako@2.1.0` / `obsidian@1.12.3`（注意：registry 里**没有** `obsidian@1.4.16`、`typescript@5.3.0` 这两个包版本，照写会 ETARGET；npm 直接装 latest 会给你 **typescript 7.x**）。
- 在托管工作区 `npm install --no-save <个别包>` 会把**没列进这次命令行**的已装包当 extraneous 清掉（第一次装 esbuild 时把 obsidian / typescript 一起清了）→ 要装就一次把需要的包全列上。
- 用 `--no-save` 装包的那条命令可能被沙箱 SIGTERM 掉、日志为空，但**包其实已经装好了**，别急着重试，先验版本。

**发版（用户指令后执行）**
- 版本 `0.18.2 → 0.18.3`（patch：纯内部重构，无行为变更），三处同步：`manifest.json` / `package.json` / `versions.json` 追加 `"0.18.3": "1.4.0"`。
- 守门：`node .github/scripts/check-version.mjs --tag 0.18.3` → 通过（0 提示）。
- 重跑 `tsc --noEmit` + `esbuild --production` 后提交：`54b6618 fix: 内联样式改走 CSS 类与自定义属性，清空 no-static-styles-assignment (v0.18.3)`，随后 `docs(memory)` 一个提交。
- 已推 `main` 到 `origin`（`git@github.com:hellokunzai/obsidian-drawio-studio.git`）并推 tag `0.18.3` → 触发 `release.yml` 建 Release（附 main.js / manifest.json / styles.css）。
- 注意 `beta.yml` 只在 commit message 含 `[beta]` / `[rc]` 时发预发布，普通提交不触发；`release.yml` 由 tag push 触发。

